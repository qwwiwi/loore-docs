---
title: "Инструменты MCP"
description: "Полный список инструментов, которые агент получает после подключения Loore MCP."
---

{/* GENERATED by scripts/generate.mjs from the live OpenAPI — do not edit by hand. */}

Сервер публикует **32** инструментов. Имя инструмента в MCP и `operationId` в REST — это одно и то же: подключился по MCP — зовёшь `ig_analytics`; пошёл через REST — делаешь `POST /v1/ig-analytics`. Одна и та же операция, один и тот же ключ.

<Info>
  Список обновляется автоматически из живого контракта API. Если инструмента здесь нет — его нет и на сервере.
</Info>

| Инструмент (MCP) | REST | Что делает |
| --- | --- | --- |
| `analyze_reel` | `POST /v1/analyze-reel` | Pull the RAW data of ONE Instagram reel by its link: timecoded transcript, metrics (plays/likes/comments), the author-baseline outlier score vs their own median, and — on depth=deep — the sampled video FRAMES as timecoded images (hook/CTA/scene) for YOUR OWN agent to read visually and break down. |
| `artifact_list` | `POST /v1/artifact-list` | List the images the user has generated (covers and carousel slides), newest first, for their gallery. |
| `download_instagram_media` | `POST /v1/download-instagram-media` | Download and SEND the actual video/photo FILE of an Instagram post or reel to the user in this chat. |
| `download_instagram_story` | `POST /v1/download-instagram-story` | Download and SEND the actual video/photo FILE of an Instagram STORY to the user in this chat. |
| `ig_analytics` | `POST /v1/ig-analytics` | Run an Instagram analysis for a public handle and return its RAW computed metrics (profile, engagement, content mix, top reels/carousels, posting windows, hashtags). |
| `ig_analytics_read` | `GET /v1/ig-analytics/{id}` | Read a single Instagram analysis result by id. |
| `ig_audit_render` | `POST /v1/ig-audit-render` | Turn a finished Instagram account analysis into a BRANDED, ready-to-send audit report FILE (HTML) and return its link. |
| `ig_followers_sample` | `POST /v1/ig-followers-sample` | Fetch a SAMPLE (first page, up to 30) of a public account's followers OR the accounts it follows — handles plus display names. |
| `ig_highlights` | `POST /v1/ig-highlights` | List a public Instagram account's Story Highlights (up to 20): each highlight's title, how many items it holds, and a cover image URL when available. |
| `ig_music_trends` | `POST /v1/ig-music-trends` | Search Instagram's audio library for trending sounds to use in Reels. |
| `ig_post_engagement` | `POST /v1/ig-post-engagement` | Read the engagement depth of a single public post or reel: its like/comment totals, a sample of up to 20 accounts who liked it, and the top comment threads (up to 10, each with up to 3 replies taken from the thread's preview — not the full reply history; text trimmed). |
| `ig_request` | `POST /v1/ig-request` | Advanced Instagram data lookup for anything the dedicated tools don't already cover — pick one of the supported endpoints and pass its parameters. |
| `ig_stories` | `POST /v1/ig-stories` | Look up a public Instagram account's currently-live Stories (metadata only — no media is downloaded). |
| `kb_search` | `POST /v1/kb-search` | Search the user's OWN knowledge base for passages relevant to a query, to ground your answer in their uploaded documents. |
| `library_list` | `POST /v1/library-list` | Read the user's own library: the searches they kept (with how often they returned to each one and when it last ran) and the profiles, reels, posts and hashtags they put aside with their notes. |
| `library_save_item` | `POST /v1/library-save-item` | Put one thing aside on the user's library shelf: a profile, a reel, a post or a hashtag. |
| `library_save_search` | `POST /v1/library-save-search` | Keep a search on the user's own library shelf so they can return to it later: what was searched (accounts, reels, posts or hashtags) and the exact query. |
| `mcp_json_rpc` | `POST /mcp` | Model Context Protocol (MCP) JSON-RPC 2.0 endpoint — the URL you paste into Claude/Codex. |
| `miner_fired_posts` | `POST /v1/miner-fired-posts` | List which of the user's tracked competitor POSTS fired recently — photos and carousels whose likes suddenly outran their author's usual numbers — newest fire first. |
| `miner_fired_reels` | `POST /v1/miner-fired-reels` | List which of the user's tracked competitor reels fired recently — clips that suddenly outperformed their author's usual numbers — newest fire first. |
| `miner_insights` | `POST /v1/miner-insights` | Read everything the user has mined about a niche. |
| `miner_search` | `POST /v1/miner-search` | Find what's going viral in an Instagram niche: start a background scan that mines top public creators and their best-performing reels for a niche you name, building the user's competitive base to analyze. |
| `miner_track` | `POST /v1/miner-track` | Add a competitor to the user's daily tracking so their new reels are watched for breakouts. |
| `plan_checkout` | `POST /v1/plan-checkout` | Create a hosted payment checkout for upgrading the key owner to a paid plan. |
| `reference_delete` | `POST /v1/reference-delete` | Delete the user's stored reference assets. |
| `reference_list` | `POST /v1/reference-list` | List the user's stored visual reference assets (id, kind, label, dimensions, size, created/last-used timestamps), newest first. |
| `reference_save` | `POST /v1/reference-save` | Save the user's supplied images as visual reference assets — face photos, cover-style references, or a logo — to reuse in future image generations. |
| `report_render` | `POST /v1/report-render` | Turn a finished analysis into a BRANDED, ready-to-send report FILE from GENERIC structured content (title, metric tiles, markdown sections + tables) and return its link(s). |
| `skill_create` | `POST /v1/skill-create` | Create a new custom skill for the key owner from a config: a name, a url-safe slug (its /command), a one-line description, the inputs to gather (JSON-schema object), the pipeline steps (each an id + an allowlisted stage + a templated input), and the output projection. |
| `skill_list` | `POST /v1/skill-list` | List the skills the user can run — reusable, named recipes. |
| `skill_run` | `POST /v1/skill-run` | Run one of the user's skills by its slug, passing the inputs it declared. |
| `skill_run_status` | `POST /v1/skill-run-status` | Check a skill run by its runId. |

## Как вызвать

<CodeGroup>

```bash REST
curl -X POST https://mcp.loore.ai/v1/ig-analytics \
  -H "Authorization: Bearer $LOORE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"handle":"@brand"}'
```

```text MCP
Подключи https://mcp.loore.ai/mcp с заголовком Authorization: Bearer <ключ>
и вызови инструмент ig_analytics с аргументом handle.
```

</CodeGroup>

Ответ всегда приходит в одном конверте: `{ status, error?, details?, result? }`.
